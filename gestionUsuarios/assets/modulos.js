/**
 *
 * @param {number} num1
 * @param {number} num2
 * @returns {number}
 */
export const sumar = (num1, num2) => num1 + num2;
/**
 * 
 * @param {} variable 
 * @returns 
 */
export const mostrar = (variable) => console.log(variable);
/**
 *
 * @param {string} nombre
 * @returns {string}
 */

export default function saludar(nombre) {
  return `Hola, ${nombre}`;
}
